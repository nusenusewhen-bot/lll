[phases.setup]
nixPkgs = ["python311", "nodejs_18", "gcc", "tesseract", "sqlite"]

[phases.install]
cmds = [
    "python -m venv /app/venv",
    "/app/venv/bin/pip install --upgrade pip",
    "/app/venv/bin/pip install -r requirements.txt",
    "npm install"
]

[phases.build]
cmds = []

[start]
cmd = "/app/venv/bin/python bot.py"
